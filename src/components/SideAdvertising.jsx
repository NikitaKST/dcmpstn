// Компонент для отображения боковой рекламы.
// Принимает заголовок title, текст text и ссылку на изображение img через пропсы.
import React from 'react';

export default function SideAdvertising({ title, text, img }) {
  return (
    <div>
      {/* Разметка для отображения рекламы */}
    </div>
  );
}
